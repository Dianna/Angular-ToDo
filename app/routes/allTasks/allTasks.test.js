'use strict';

describe('myApp.allTasks module', function() {

  beforeEach(module('myApp.allTasks'));

  describe('allTasks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var allTasksCtrl = $controller('AllTasksCtrl');
      expect(allTasksCtrl).toBeDefined();
    }));

  });
});
