'use strict';

describe('myApp.complete module', function() {

  beforeEach(module('myApp.complete'));

  describe('complete controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var completeCtrl = $controller('CompleteCtrl');
      expect(completeCtrl).toBeDefined();
    }));

  });
});
